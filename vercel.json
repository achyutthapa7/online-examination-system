{
  "version": 2,
  "builds": [
    {
      "src": "admin/package.json",
      "use": "@vercel/node"
    },
    {
      "src": "admin/**",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "admin/dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/admin/$1"
    }
  ]
}
